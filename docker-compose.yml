services:
  frontend:
    build:
      context: .
      args:
        REACT_APP_API_URL: ${REACT_APP_API_URL:-https://examroomedu.com/2/api}
        PUBLIC_URL: /2
    ports:
      - "80:80"
    restart: unless-stopped
